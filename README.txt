وكالة البالة - النسخة السحابية v2

هذه النسخة تستخدم PostgreSQL عبر خادم Node.js ولا تخزن البيانات الحساسة في المتصفح.

متغيرات البيئة المطلوبة في غيمة:
PGHOST
PGPORT=5432
PGUSER
PGPASSWORD
PGDATABASE
PGSSL=true

لا تضع كلمة مرور قاعدة البيانات داخل GitHub أو داخل index.html.
