# ALMADINAH — نقطة حفظ ومراجعة 19 سبتمبر 2026

## النسخ المحفوظة

المراجع أدناه فروع حفظ مستقلة على GitHub. يُستخدم `main` للتطوير، وتبقى فروع الحفظ عند نقاطها الأصلية.

| المرجع | ما يحفظه |
|---|---|
| `almadinah-baseline-2026-09-15` | النسخة التي اعتمدها المستخدم سابقًا: `d5ea927897f5ce42113bf64eaf22509360fb1355` |
| `almadinah-preserved-2026-09-17` | كود `main` بعد إضافة حذف دفعات المورد: `2437e915b2574f79d1274fd941944e84e270ffdb` |
| `almadinah-checkpoint-2026-09-19` | نقطة الحفظ على `main`: الكود نفسه مع تقرير المراجعة والاختبارات وتحديث اختبار التشغيل فقط |

هذه نقطة استعادة للكود الذي اعتمده المستخدم، مع ملاحظات معلنة أدناه. ليست شهادة بأن جميع الحالات خالية من الأخطاء. لم تُنفّذ المراجعة أي حركة على بيانات العمل الحقيقية، ولم تغيّر متغيرات غيمة أو تعِد نشر التطبيق من أدوات الاستضافة.

مجلد المشروع لا يحتوي قاعدة البيانات الحية أو أسرار الاستضافة. فروع الحفظ في Git تحفظ الكود؛ استعادة البيانات تحتاج نسخة قاعدة بيانات مستقلة ومفاتيح فك تشفيرها، ولم يتم إنشاء نسخة بيانات أو التحقق من النسخ المجدولة ضمن هذه المراجعة.

## المقارنة الفعلية

المرجعان السابق والحالي متفرعان عن `297627549d59d6342da9731c9b755f0af319be7a`، لذلك أُجريت مقارنة مباشرة لمحتوى النسختين باستخدام:

```bash
git diff d5ea927897f5ce42113bf64eaf22509360fb1355 2437e915b2574f79d1274fd941944e84e270ffdb
```

المقارنة المباشرة تحصر الفرق في **4 ملفات، 91 سطرًا مضافًا و10 أسطر محذوفة**. مقارنة GitHub من نقطة الالتقاء وحدها قد توحي بأن نموذج المخزون أضيف حديثًا؛ لكنه موجود بالفعل في النسخة السابقة.

| الملف | التغيير منذ النسخة السابقة |
|---|---|
| `inventory_add_form_patch.js` | إضافة تعليق توضيحي فقط؛ سلوك الاسم العربي والكمية لم يتغير |
| `launcher.js` | تغيير نسخة تحميل نموذج المخزون من `v=1-safe` إلى `v=1`، وتحميل تعديل المورد بـ `v=4-delete-payment` بدل `v=3-no-delete` |
| `supplier_account_patch.js` | زر حذف للدفعات فقط، رسالة تأكيد بالمبلغ والتاريخ، وتحديث الكشف بعد النجاح |
| `supplier_account_routes.js` | مسار DELETE للدفعة وإعادة حساب أرصدة الحركات والإجماليات ورصيد المورد؛ استخدام إعادة الحساب أيضًا عند إضافة حركة |

ملفات البيع، الإرجاع، كشوف الزبائن، المخزون الخلفية، قاعدة الاتصال، واتساب، والنسخ الاحتياطي مطابقة للنسخة السابقة. لا توجد ترحيلات قاعدة بيانات في الفرق.

## نتائج المراجعة — تحتاج معالجة منفصلة

### P1: الحذف بموقع الحركة قد يحذف دفعة أخرى

الموقع: `supplier_account_routes.js`، المسار `/api/supplier-account/:id/entries/:entryIndex`، و`deletePayment` في `supplier_account_patch.js`.

أُعيد إنتاج الحالة: كشف تجريبي يحتوي رصيدًا افتتاحيًا 100 دولار، مشتريات 50، ثم دفعتين 10 و20. حذف الحركة رقم 2 يحذف دفعة 10 ويرفع الرصيد من 120 إلى 130 كما يجب. تكرار **نفس الطلب** ينجح أيضًا، لكنه يحذف دفعة 20 التي انتقلت إلى الموقع نفسه، ويرفع الرصيد إلى 150.

هذا ممكن عند إعادة الطلب أو استخدام كشف قديم بعد تعديل من جهاز آخر. التأكيد في الواجهة يعرض الحركة المحلية ولا يثبت هويتها على الخادم. القراءة ثم كتابة كامل الكشف تفتقر أيضًا إلى شرط إصدار يحمي من تعارض التعديلات؛ لم يُجرَ اختبار ضغط متزامن شامل.

المعالجة المقترحة: معرّف ثابت لكل حركة مع ترحيل متوافق للكشوف القديمة، وطلب حذف يحمي من التكرار ويتحقق من إصدار الكشف عند الكتابة. تعطيل الزر أثناء التنفيذ وحده لا يكفي. هذه الملاحظة مستحدثة مع مسار الحذف؛ لم يُغيّر كود التطبيق ضمن نقطة الحفظ.

### P2: إعادة محاولة إضافة دفعة بالات بعد انقطاع قد تكرر جزءًا منها — سابق للتعديل

الموقع: `inventory_add_form_patch.js`، حلقة الحفظ داخل `saveBaleWithQuantity`.

أُعيد إنتاج الحالة: طلب إضافة 3 بالات، نجاح الأولى ثم فشل الثانية. إعادة المحاولة تضيف 3 أخرى بمعرّفات جديدة، ليصبح المضاف 4 بدل 3. الواجهة تعرض خطأ عامًا ولا تحفظ تقدم العملية. سلوك هذا الملف مطابق للنسخة السابقة عدا التعليق.

المعالجة المقترحة: معرّف عملية ثابت مع استئناف العناصر غير المحفوظة، أو إدخال جماعي ذري. عند انقطاع عملية حالية، يلزم التحقق من المخزون قبل إعادة الإدخال كاملًا.

## الاختبارات

أُجريت الاختبارات باستخدام Node.js 24.19.0. شُغّل `launcher.js` الحقيقي وخادم HTTP محلي، واستُبدلت طلبات Supabase بمخزن مؤقت داخل الذاكرة في عملية الاختبار. يمنع ملف التجهيز كل طلب شبكة صادر إلى أي عنوان آخر، ولا تُورَّث مفاتيح الإنتاج. استُخدمت بيانات اصطناعية فقط.

النتيجة: **10 اختبارات ناجحة، وحالتا TODO تعيدان إنتاج الخللين الموثقين أعلاه**. حالة TODO ليست نجاحًا وظيفيًا؛ خروج الأمر برمز 0 لا يلغي الملاحظتين.

| المجال | ما جرى التحقق منه |
|---|---|
| التشغيل | تحميل ملفات المورد والمخزون والكشوف بالنسخ المتوقعة، وإيقاف التكاملات الخارجية في بيئة الاختبار |
| المورد | حذف دفعة وسطية وإعادة كل الأرصدة التالية والإجماليات، ورفض حذف الافتتاحي والمشتريات والمعرّفات غير الصحيحة |
| العزل | حذف دفعة المورد لا يغيّر البالات أو مبيعات الزبائن أو دفعاتهم أو ديونهم أو الحركات النقدية |
| إضافة حركة للمورد | دقة الإجماليات العشرية بعد الحذف ثم إضافة دفعة ومشتريات |
| المخزون | حفظ 3 بالات باسم عربي فقط، ظهور الكمية نفسها في المخزون وقائمة البيع والتحميل السريع، ورفض أكثر من 500 أو غياب الشحنة |
| البيع والكشف | المعاينة لا تكتب بيانات؛ بيع بالتين بـ200 ودفع 50 يزيد دينًا افتتاحيًا 100 إلى 250، ويقلل المتاح؛ إعادة نفس البيع لا تكرره |
| الإرجاع الكامل | إعادة البالتين والدين إلى 100 وتسجيل رد نقدي 50 مرة واحدة |
| الإرجاع الجزئي | إعادة بالة بقيمة 100 إلى المخزون وتخفيض الدين إلى 150 وظهور المرتجع مرة واحدة |
| واجهة المورد | ظهور أزرار الحذف للدفعات فقط، الإلغاء دون طلب حذف، والتحديث بعد النجاح |

اختبارات الواجهة تشغّل الدوال الفعلية داخل بيئة DOM مصغرة؛ لا تمثل اختبارًا بصريًا كاملًا على iPhone. عمليات قاعدة البيانات وRPC محاكاة، لذا لا تثبت مخطط Supabase الحي أو صلاحياته أو المعاملات الفعلية أو النسخة المنشورة حاليًا على غيمة.

### اختبار التشغيل القديم

فشل `npm test` قبل إعداد نقطة الحفظ لأن `smoke_test.js` كان يتوقع `statement_share_patch.js?v=1` بينما التطبيق يستخدم `v=2-almadinah`، وهو اختلاف موجود أيضًا في النسخة السابقة. تم تحديث التوقع داخل **الاختبار فقط** وعزل بيئته عن مفاتيح الإنتاج. لم يتغير ملف التشغيل أو الواجهة بسبب ذلك.

### إعادة الاختبارات

```bash
npm ci --ignore-scripts
npm test
node --test tests/checkpoint-review.test.cjs
```

ملف `tests/checkpoint-fixture.cjs` مخصص للتحميل المسبق في الاختبار فقط، وليس لإعداد أي قاعدة بيانات حقيقية.

## استعادة نسخة واضحة دون إعادة كتابة تاريخ main

عرض معرّفات فروع الحفظ:

```bash
git fetch origin
git rev-parse origin/almadinah-baseline-2026-09-15^{commit}
git rev-parse origin/almadinah-preserved-2026-09-17^{commit}
git rev-parse origin/almadinah-checkpoint-2026-09-19^{commit}
```

للفحص أو النشر من نسخة محفوظة، أنشئ فرع استعادة من مرجع الحفظ المقصود:

```bash
git switch -c recovery/almadinah-2026-09-17 origin/almadinah-preserved-2026-09-17
```

لإعادة محتوى `main` لاحقًا إلى الكود المحفوظ مع إبقاء سجل التعديلات، نفّذ من نسخة عمل نظيفة، راجع الفرق والاختبارات، ثم ادمج فرع الاستعادة عبر Pull Request:

```bash
git fetch origin
git switch -c restore/almadinah-preserved origin/main
git restore --source=origin/almadinah-preserved-2026-09-17 --staged --worktree .
git diff --cached --stat
git diff --cached
# بعد مراجعة المحتوى وإعادة الاختبارات:
git commit -m "Restore ALMADINAH application to preserved 2026-09-17 version"
git push -u origin restore/almadinah-preserved
```

استعادة الكود لا تعكس دفعات أو مبيعات أو تغييرات مخزون سبق تسجيلها في قاعدة البيانات. لا تستخدم `push --force` أو تحذف فروع الحفظ أو تستخدمها للتطوير اليومي.

عند الفحص في 19 سبتمبر، لم يكن `main` محميًا، ولم تكن هناك rulesets أو فحوص GitHub مسجلة على `2437e91`. إنشاء فروع الحفظ والتقرير لا يفعّل حماية الفروع. يمكن لاحقًا تطبيق حماية تمنع الحذف والدفع القسري وتشترط مراجعة التعديلات، مع صلاحيات الإدارة المناسبة.
